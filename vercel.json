{
  "version": 2,
  "buildCommand": "corepack enable && pnpm install && pnpm run build",
  "outputDirectory": "dist",
  "framework": null,
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/api/cron",
      "destination": "/api/cron.ts"
    }
  ]
}